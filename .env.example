# Configuración de Base de Datos
DB_URL=jdbc:mysql://localhost:3306/biblioteca_db?useSSL=false&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=tu_password_aqui

# Configuración JWT
JWT_SECRET=cambiarEstePorUnaClaveMuySeguraDeAlMenos256BitsParaProduccion
JWT_EXPIRATION=86400000

# Notas:
# - JWT_SECRET debe tener al menos 256 bits (32 caracteres) para HS256
# - JWT_EXPIRATION está en milisegundos (86400000 = 24 horas)
# - Nunca commitear credenciales reales al repositorio
